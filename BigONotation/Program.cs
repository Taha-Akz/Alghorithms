void log (int [] num)
{
    // O(n)
    for (int i = 0; i < num.lench; i++)
    {
        System.Console.WriteLine(num[i]);

    }
}
void log(int[] num)
{
    // RuntimeComlexity = O(n) + O(1)+ O(1) ==> O(n) When we have an
    // Linear (it means Bu count of inputs it changes)  runtime complexity how much our inputs become more that much change
    System.Console.WriteLine("j");
    for (int i = 0; i < num.lench; i++)
    {
        System.Console.WriteLine(num[i]);

    }
    System.Console.WriteLine("d");
}

void SayHello ()
{
    //O (1)
    System.Console.WriteLine("Hello Taha ! ");
}